# OS
.DS_Store
Thumbs.db

# Editor
.vscode/
.idea/
*.swp

# Claude Code — internal/local only (never commit; Netlify serves the repo root publicly)
.claude/
CLAUDE.md

# Local-only working area (mirror, snapshots, archive) — never commit / never deploy
_lokal/

# Secrets / env
.env
.env.*
secrets/

# Logs / temp
*.log
node_modules/
