# OS
.DS_Store
Thumbs.db

# Editor
.vscode/
.idea/
*.swp

# Claude Code — internal/local only (never commit; Netlify serves the repo root publicly)
.claude/
CLAUDE.md

# Local dated snapshots created on each push (backup only — must stay out of the repo)
website dateien/

# Secrets / env
.env
.env.*
secrets/

# Logs / temp
*.log
node_modules/
